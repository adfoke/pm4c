#include <stdio.h>
#include <log4c.h>

int main() {
    printf("Running pm4c example with log4c...\n");
    
    // Assuming log4c has some init or log function.
    // Since I don't know the API yet, I'll just include the header.
    // If the build fails, I'll inspect the fetched source.
    
    return 0;
}
